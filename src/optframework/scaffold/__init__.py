"""
Scaffold de problema novo — gera `problems/<nome>/` num projeto consumidor.

Ver `optframework.scaffold.cli` (entry point `optframework-new`). Só usado depois que o
projeto já declarou `opt-base-framework` como dependência (`uv add git+...`) — este módulo
não instala nada, só gera a estrutura de pastas/arquivos que hoje era copiada à mão a partir
de `problems/exemplo_knapsack/` no repo do framework.
"""
